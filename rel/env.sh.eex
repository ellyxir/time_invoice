#!/bin/sh
# Disable distribution - this is a CLI tool, not a distributed system
export RELEASE_DISTRIBUTION="none"

# Use writable directory for release tmp
export RELEASE_TMP="${XDG_RUNTIME_DIR:-/tmp}"

# Set a dummy cookie since we're not using distribution
export RELEASE_COOKIE="time_invoice_no_distribution"
